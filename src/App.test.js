import { render, screen } from '@testing-library/react';
import App from './App';

describe('test', () => {
  it('should', () => {
    expect(true).toBe(true)
  })
})

describe('CounterClass test', () => {
  it('should pass', () => {
    expect(true).toBe(true)
  })
})